import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    marginTop: 64
  },
  addButton: {
    paddingTop: 4,
    paddingLeft: 3,
    fontSize: 20
  }
})

export default styles
